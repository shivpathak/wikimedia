apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-chart-wikimedia.fullname" . }}
  labels:
    {{- include "helm-chart-wikimedia.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-chart-wikimedia.selectorLabels" . | nindent 4 }}
